import { TeamRocketReturn } from '@/components/TeamRocketReturn';

export function NotFound() {
  return (
    <div className="flex h-screen flex-col items-center justify-center gap-2">
      <TeamRocketReturn />
    </div>
  );
}
